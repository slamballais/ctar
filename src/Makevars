PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

# Disable optimization and add debug symbols for coverage builds
ifdef COVR
  ifeq ($(UNAME_S),Linux)
    PKG_CFLAGS = -g -O0
  endif
  ifeq ($(UNAME_S),Darwin)
    PKG_CFLAGS = -g -O0
  endif
else
  PKG_CFLAGS = -O2
endif
